# Miscellaneous

The topics that used to live on this page now have their own, easier-to-find pages:

- **Bundling arguments (`myargs`)** — fill shared arguments once and reuse them across
  functions → [Bundling arguments](bundled_arguments.md).
- **Verbose & progress switches** — global control of messages and progress bars, including
  the [`output_mode`](@ref) master switch → [Verbose & progress switches](verbose_progress_switches.md).
- **Notifications** — `bell`, `notifyme` (e-mail / Zulip) → [Notifications](notifications/index.md).
